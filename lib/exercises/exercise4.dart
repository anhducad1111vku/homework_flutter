import 'package:flutter/material.dart';

class Exercise4 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Exercise 4'),
      ),
      body: const Center(
        child: Text('This is Exercise 4: Buttons and Icons'),
      ),
    );
  }
}
