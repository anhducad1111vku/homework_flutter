import 'package:flutter/material.dart';

class Exercise5 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Exercise 5'),
      ),
      body: const Center(
        child: Text('This is Exercise 5: Lists and Grids'),
      ),
    );
  }
}
