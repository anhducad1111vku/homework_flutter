import 'package:flutter/material.dart';

class Exercise2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Exercise 2'),
      ),
      body: const Center(
        child: Text('This is Exercise 2: Basic Widgets'),
      ),
    );
  }
}
