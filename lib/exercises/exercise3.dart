import 'package:flutter/material.dart';

class Exercise3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Exercise 3'),
      ),
      body: const Center(
        child: Text('This is Exercise 3: Layouts'),
      ),
    );
  }
}
