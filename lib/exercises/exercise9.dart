import 'package:flutter/material.dart';

class Exercise9 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Exercise 9'),
      ),
      body: const Center(
        child: Text('This is Exercise 9: Animations'),
      ),
    );
  }
}
