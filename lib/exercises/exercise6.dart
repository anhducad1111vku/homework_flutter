import 'package:flutter/material.dart';

class Exercise6 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Exercise 6'),
      ),
      body: const Center(
        child: Text('This is Exercise 6: Forms and Input'),
      ),
    );
  }
}
